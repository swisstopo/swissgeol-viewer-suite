import { Given, Then, When } from '@badeball/cypress-cucumber-preprocessor';

Given(/^the topographic map is displayed in the 3D scene$/, function () {

});
When(/^the user clicks on the background chip$/, function () {

});
Then(/^the background switcher opens below the background layer in the data displayed section$/, function () {

});
Then(/^the three options are displayed$/, function () {

});
When(/^the user clicks on the left thumpnail$/, function () {

});
Then(/^the background map changes to swissimage orthophoto$/, function () {

});
When(/^the user clicks on the middle thumpnail$/, function () {

});
Then(/^the background map changes to topographic map$/, function () {

});
When(/^the user clicks on the right thumpnail$/, function () {

});
Then(/^the background map changes to rivers and lakes and transparent background$/, function () {

});
When(/^the user clicks on the background chip again$/, function () {

});
Then(/^the background switcher collapses$/, function () {

});
