{
  "name": "swissgeol-viewer-app",
  "private": true,
  "description": "Root package for shared development tools",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "*.{js,ts,mjs,cjs,json,json5,yaml,yml,md,html,css}": "prettier --write"
  }
}
